version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - coinor-libipopt-dev
  jobs:
    post_install:
      - pip install -e ".[docs,jax,ipopt]"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

formats:
  - htmlzip
